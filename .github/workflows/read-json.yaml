name: Read JSON File

on:
  workflow_dispatch: # Manual trigger

jobs:
  read-json:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Install Node.js
      # - name: Set up Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '18'

      # # Step 3: Install dependencies (if any)
      # - name: Install dependencies
      #   run: npm install

      # Step 4: Read and log JSON file
      # - name: Read JSON File
      #   run: |
      #     node -e "const fs = require('fs'); const data = JSON.parse(fs.readFileSync('./data.json', 'utf8')); console.log(data);"
       - name: Pretty-print JSON using jq
        run: jq '.' ./data.json